# 几何生产工具

开源的几何说明书编辑器：一个页面，视口可在 2D / 3D 间切换。操作员对照题图画出结构，交给 Agent 去生成互动课件。本仓库不是课件本身，也不是老师或学生用的教具沙盘。

## Language

**操作员**:
对照题图画出几何结构、把结果交给 Agent 的人。可以是本团队，也可以是使用本开源工具的其他人。
_Avoid_: 用户, 老师, 学生

**课件**:
下游用 p5 / three 生成的互动数学课。本工具不产出课件。
_Avoid_: 游戏（单独使用时）, 教具课, Geometry Studio（那是本团队的下游系统，不是这个词）

**题图**:
题目的原图。操作员画结构时的真值来源。
_Avoid_: 截图, 参考图

**垫图**:
视口里半透明铺在图元底下的题图，只给操作员对齐。不把题图像素当作交给 Agent 的几何。分享链接只带垫图的 http(s) 地址和对齐；本地上传的图对方看不到。
_Avoid_: 背景, 水印

**Agent**:
根据说明书复原图形并生成课件的 AI。
_Avoid_: 模型, LLM（在本上下文中）

**说明书**:
一份描述图形结构的 JSON。操作员画完后交给 Agent 的真源。可以是本地文件，也可以编码进分享链接。
_Avoid_: IR, 画布状态, 工程文件, Prompt（Prompt 不是真源）, 云文档

**分享链接**:
把说明书放进 URL 里、别人打开即看到同一份结构的链接。没有账号，也没有服务器上的文档。
_Avoid_: 短链后台, 云同步, 协作房间

**Prompt**:
说明书的可读投影，给人粘贴给 Agent。内容必须能从说明书生成，不能反向当真源。
_Avoid_: 提示词（单独当作产物时）

**编辑器**:
单一页面的生产界面。由可停靠面板组成：工具箱、对象列表、视口、属性面板、垫图。视口可在 2D / 3D 间切换。界面中英双语（标准 i18n），默认跟浏览器语言，中英之外则中文；说明书字段名永远英文。
_Avoid_: 三个独立工具, Unity（只借壳，不是 Unity 项目）

**工具**:
编辑器当前的鼠标模式。选择工具点选已有图元，并通过变换手柄平移、旋转、缩放；创建工具在视口里新画一种图元。单次完成的创建工具（2D 全部图元与 3D 参数体）提交后自动切回选择，刚画的图元保持选中；连续放置的体素不切回。没有修饰键连续绘制，也没有锁定模式——想再画一个就再按一次工具快捷键。
_Avoid_: 状态, 模式, 粘性工具（本编辑器不采用 Unity 式粘性）

**工具箱**:
列出选择工具和可创建的图元类型（图标 + 名称）。2D 列出平面图元；3D 列出选择、体素与参数体。点一项即切换当前工具。创建工具按工具分组归入小节，选择置顶独立一段。与对象列表分开，不是说明书里已有图元的清单。
_Avoid_: 图形列表, 形状面板, 工具栏（单指这一块时）

**工具分组**:
工具箱里创建工具的小节归类，纯展示层概念：分组不进说明书、不动快捷键、不改工具切换逻辑。2D 五组，按结构优先次序——线与弧、直边图形、曲线图形、度量与标注、特殊；3D 两组——体素、参数体。操作型/引用型工具（重叠填充、拆分）归「特殊」组。选择工具不归任何组。每个创建工具必属一组（目录里类别必填、穷尽检查），不设「其他」垃圾抽屉组；小节不可折叠。
_Avoid_: 图形标签（指逐项彩色 tag——被否掉的另一形态）, 分类（泛指时）

**工具快捷键**:
切换工具的单键或 Shift+键。单键给高频工具，Shift+键给同族变体；只切当前空间里存在的工具，输入文字时不触发。键位表见 `docs/adr/0018-auto-return-to-select.md`。
_Avoid_: 热键, 加速键

**变换手柄**:
选择工具下，选中后出现的整图元控件。2D 图元与 3D 参数体：拖本体平移，拖缩放柄缩放，拖旋转柄旋转；指针悬停时切换光标。3D 体素没有变换手柄，拖动是整格平移。
_Avoid_: Gizmo, 操纵器, 包围盒（只描述缩放框时）

**控制点**:
选中图元上可拖的几何锚点。2D 如端点、圆心、半径点、弧的起止角、多边形顶点；3D 参数体如长宽高、半径、高。拖它只改这一处几何。体素没有控制点。命中顺序：控制点优先于变换手柄，再其次才是拖本体平移。
_Avoid_: 顶点（顶点只是折线/多边形的控制点）, 锚点

**选中**:
编辑器里当前正在操作的那一条图元。视口、对象列表、属性面板共用同一个选中；同时最多一条。
_Avoid_: 焦点, 高亮（高亮只是选中的画法）

**对象列表**:
说明书里已经存在的图元清单。点一项即选中。不是工具箱。
_Avoid_: Hierarchy, 图层

**显示名**:
对象列表与属性面板里图元的展示称呼：当前语言下的类型名复用工具箱词条，同类型按说明书顺序编号——首个不加号（圆），其后 圆1、圆2。展示层实时推导，删除后自动收紧，不写进说明书；图元的身份永远是 id，悬停可见。
_Avoid_: 图元名（当作持久字段时——说明书没有名字字段）, UUID（显示给操作员看时）

**属性面板**:
展示并编辑当前选中图元的字段。未选中时为空。改这里的值和视口里拖动手柄是同一份说明书。垫图不在这里。
_Avoid_: Inspector, 侧栏（侧栏可能同时停着好几块面板）

**面板**:
编辑器里一块可拖拽停靠的区域。第一期五块：工具箱、对象列表、视口、属性面板、垫图。可以并排、叠成标签、改大小。
_Avoid_: 窗口, Widget, 分栏（分栏只是某一次摆放）

**空间**:
一份说明书所属的几何世界，取值只能是 2D 或 3D。切换视口即切换这份说明书的空间，同一份里不混放平面图元和立体图元。
_Avoid_: 场景, Scene

**视口**:
编辑器中央的绘制区。2D 为正交坐标，3D 为轨道相机。两套空间都是 Y 向上。
_Avoid_: 画布（单独当作产品概念时）, Game 视图

**高度**:
Y 轴。2D 与 3D 都是 Y 向上；3D 的地面在 XZ。与 Unity、Three.js 一致。
_Avoid_: Z 为高度（旧体素工具的习惯）

**图元**:
说明书里的一条几何对象。名单开放，按题图需求增补（现行名单见 `docs/todo.md`）。
_Avoid_: 形状, 教具（教具盒是后期图元目录）

**三角形**:
2D 参数化图元：底、高、顶点偏移三个尺寸描述，锚点是底边中点，底沿局部 X、高沿局部 +Y，旋转绕锚点。等腰、直角、等边都是它的参数取值特例，不是独立类型。
_Avoid_: 等腰三角形 / 直角三角形 / 等边三角形（当作图元类型时）, 三棱柱（那是 3D 参数体）

**平行四边形**:
2D 参数化图元：底、高、斜移描述，锚点是底边中点。斜移为零的取值被契约拒绝——那是矩形，矩形只有一种规范表达。菱形是参数取值特例，不是独立类型。
_Avoid_: 菱形（当作图元类型时）, 长方形（当作平行四边形的特例时）

**梯形**:
2D 参数化图元：下底、上底、高、上底偏移描述，锚点是下底中点。上底为零、或与下底等长的取值被契约拒绝——那是三角形、矩形或平四的方言。等腰、直角梯形是参数取值特例，不是独立类型。
_Avoid_: 等腰梯形 / 直角梯形（当作图元类型时）

**正多边形**:
2D 参数化图元：边数（至少 5）、外接圆半径描述，锚点是中心（即外接圆心）。边数 3、4 被契约拒绝——等边三角形与正方形各有唯一规范表达。缺省朝向是平底：一条边平行局部 X 且在下方。
_Avoid_: 正方形 / 等边三角形（当作正多边形的成员时）, 正五边形 / 正六边形（当作独立图元类型时——都只是边数取值）

**角**:
2D 图元：顶点、两条边的方向角（沿圆族度数惯例）、边长描述，锚点是顶点，两边等长。弧标是它的身份，总是画出；度数数值不进图元——那是方向角的推导值，弧标旁可显示推导度数，开关随图元。无填充。
_Avoid_: 角度（当作图元类型时）, 周角（不进角的契约，用圆加两条线拼）

**尺寸标注线**:
2D 图元：定长两点描述，显示的数字是两点距离的推导值，不设文字字段，不带单位。笔画族，无填充；两个端点即控制点。
_Avoid_: 测量工具, 尺子（当作图元时）, 标注（泛指时也含点名，别混）

**度量标注**:
2D 引用型图元：一个源图元 id 加度量种类（面积或周长）描述，数值是渲染期推导值，不设文字字段、不带单位、两位小数去尾零。周长取边界全长（环含内外两圈，扇形含两半径，弓形含弦）。无坐标无锚点无控制点：面积显示在源形心，周长在源包围盒上方，源挪动跟随重绘，源删除级联删除。源只能是 2D 封闭图元（多边形、矩形、三角形、平行四边形、梯形、正多边形、圆、扇形、弓形、圆环、椭圆）；重叠填充与笔画族被拒——交集无几何表达，笔画无面积。创建走两步拾取，提交后切回选择。点中数字即选中标注并高亮其源。
_Avoid_: 测量工具（工具箱里叫面积标注 / 周长标注）, 度量值（当作存储字段时——数值永不进契约）

**重叠填充**:
2D 图元：两个封闭图元的 id 加一份填充样式描述，几何不进说明书——交集是渲染时离屏合成的推导值，曲边因此精确（月牙形没有参数形表达，烘焙即折线近似）。无锚点无控制点，不能独立拖动；源图元挪动它跟随重绘，任一源被删则级联删除。创建走两步拾取：非封闭图元、同一图元两次、两源不相交都被契约拒绝；创建后拖到不相交不报错，渲染为空但条目保留。Prompt 投影是一句关系描述（「圆 A 与三角形 B 的重叠部分有阴影」），不是坐标。
_Avoid_: 图像裁剪, 裁剪（业内指 clip mask，另有所指）, 布尔交集（当作几何运算时——它不算几何）

**拆分**:
2D 工具：把闭合直边图元沿刀具切成全部不重叠碎片。被切图元消失，碎片各是独立 polygon 图元，继承被切图元的描边与填充，可单独拖走。剪除不设——拆分后手删即可；联合明确不做——割补要的是块能分开动。曲边图元（圆、扇形、弓形、弧、圆环、椭圆）一律拒绝参与。交互两步拾取：先点被切图元，再点刀具，提交后切回选择。引擎 Clipper2（ADR 0019）。
_Avoid_: 布尔运算（当全家桶时——只做拆分一个）, 联合 / 剪除 / 相交（本编辑器不设）, 图像裁剪

**刀具**:
拆分里切别人的那一方：闭合直边图元或线段。线段切完即消失（辅助线）；闭合刀具保留不动，可能是题图真形状，删不删操作员定。刀具只切别人，自己不被切；与被切图元不足两个交点（没真正切过去）被契约拒绝。
_Avoid_: 剪刀（工具箱里的工具名叫拆分）, 切割线（线段只是刀具的一种取值）

**体素**:
3D 里占一个整数格的单位立方体。只能整格平移，不能旋转或缩放。
_Avoid_: 方块, 正方体（正方体是三边相等的长方体）

**参数体**:
3D 里用尺寸描述的图元：长方体、圆柱、圆锥、球、四棱锥、三棱柱。站立体的位置是底面中心，高沿 +Y；球的位置是球心。旋转是三个欧拉角（度），顺序 Y→X→Z。同一份 3D 说明书里可与体素共存。
_Avoid_: mesh, 模型, 实体

**格**:
图元落点的离散单位。默认边长 1，可切为 1/2；Alt 可暂时不落格。2D 图元与 3D 参数体都吃这套格；3D 体素永远整数格，不受 1/2 和「关」影响。
_Avoid_: 像素, 自由坐标（作为默认）

## 编辑精度

**数字字段统一精度**:
所有数字字段（位置、尺寸、角度、半径等）在属性面板支持小数点后 2 位精度。输入框允许用户输入小数（如 `3.75`），提交时截断到两位小数再验证。3D 体素的 x/y/z 在截断后转整数后验证。
_Avoid_: 不同字段不同精度, 无限精度输入
