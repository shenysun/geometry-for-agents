# 07 — 2D 视口（看）

**What to build:** 2D 空间下，视口用命令式 Konva 投影器按说明书画出网格、坐标轴和图元。Y 向上。可平移缩放。投影器只 `render(说明书)`，不把 Konva 树当真源。

**Blocked by:** 06 — 打开与保存 JSON；05 — 格与点选

**Status:** done

- [x] 空 2D 说明书可见网格与 X/Y 轴（Y 向上）
- [x] 打开含图元的 JSON 后视口画出对应图形
- [x] 滚轮缩放、拖拽平移；格的视觉步长随缩放可读
- [x] 不用 vue-konva 图元树；说明书变化后投影器重绘
