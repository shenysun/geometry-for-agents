# 08 — 2D 画线与多边形

**What to build:** 操作员用工具栏画 `line` 与 `polygon`：手势中只见预览，松手（或闭合）才写入说明书。可选中、删除、撤销。对象列表与视口一致。

**Blocked by:** 07 — 2D 视口（看）

**Status:** done

- [x] 拖拽画出线段，松手后列表与视口多一条 `line`
- [x] 单击加点、闭合得到 `polygon`；Esc 取消预览且说明书不变
- [x] 选中后可删；undo 一次手势一步
- [x] 绘制过程中说明书尚未改变（预览层）
