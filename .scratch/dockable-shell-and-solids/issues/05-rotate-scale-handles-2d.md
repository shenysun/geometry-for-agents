# 05 — 2D 旋转/缩放手柄与椭圆旋转角

**What to build:** 选中 2D 图元后出现旋转柄和缩放柄。拖旋转柄旋转，拖缩放柄缩放，都直接改说明书几何，不另做矩阵。圆只等比缩放。椭圆增加 `rotationDeg`（0° 即轴对齐）；打开旧的无该字段的椭圆当作 0°。扇、弓、弧绕圆心转，改 `startDeg`/`endDeg`。指针悬停在柄上时光标改变。

**Blocked by:** 04 — 选择工具拖 2D 图元平移

**Status:** done

- [x] 选中后可见旋转柄与缩放柄；拖动手势预览后一次提交
- [x] 圆缩放保持圆形；椭圆可旋转，`rotationDeg` 进说明书
- [x] 无 `rotationDeg` 的旧椭圆 JSON 能打开且角度为 0
- [x] 扇/弓/弧旋转写入起止角，圆心按整图元平移规则仍由本体拖动负责
- [x] Prompt 写明椭圆可带旋转角；hash/打开保存往返成功
