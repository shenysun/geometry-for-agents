# 可停靠面板用 dockview-vue

编辑器五块面板（工具箱、对象列表、视口、属性面板、垫图）要能拖拽停靠、并排、叠标签。Reka UI 只做按钮/对话框/无头控件，不做 IDE 分块。选用 `dockview-vue`（MIT，官方 Vue 3），不手写拖放引擎，也不用 Golden Layout。面板摆放记在本机，用 VueUse `useLocalStorage`，不进说明书、不进分享链接；提供恢复默认布局。视口不能关掉；工具箱、对象列表、属性面板、垫图可关，并从顶栏重新打开。第一期只在停靠区内并排、改大小、叠标签；不做页内浮窗，也不弹出浏览器窗口。
