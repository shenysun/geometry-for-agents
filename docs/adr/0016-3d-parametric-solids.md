# 3D 第一期含体素与参数体

3D 不只单位立方体。第一期封闭名单：体素、长方体、圆柱、圆锥、球、四棱锥、三棱柱。体素仍占整数格、不旋转不缩放；参数体用尺寸摆放，可与体素写在同一份 3D 说明书里。剖切、水面仍留第二期。创建参数体是单击落下默认尺寸，再靠变换手柄和控制点改；体素仍点格放置。站立体位置写底面中心（y 为底面高度，高沿 +Y）；球写球心；体素仍写最小角。参数体旋转为三个欧拉角（度），顺序 Y→X→Z，字段 `rotationDegY` / `rotationDegX` / `rotationDegZ`；0 表示底面朝下站着。体素无旋转字段。
