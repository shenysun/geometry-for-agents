# 变换直接改几何字段，椭圆带旋转角

选择工具的平移/旋转/缩放写入说明书里的几何，不另做一层 position/rotation/scale 矩阵。椭圆增加 `rotationDeg`（0° 即轴对齐）。圆只等比缩放，保持为圆。扇/弓/弧绕圆心改 `startDeg`/`endDeg`。3D 参数体同样直接改几何（含朝向与尺寸）；体素不旋转、不缩放，只整格平移。选中后同时显示控制点与变换手柄，命中顺序：控制点 > 旋转/缩放柄 > 拖本体。
