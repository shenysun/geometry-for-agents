# 后期（第一期明确不做）

第一期图元见下节。这里只记已经拍板延后的事，避免范围回潮。

## 第一期图元

**2D：** 线段 / 折线、多边形、矩形、三角形、平行四边形、梯形、正多边形（边数 ≥ 5）、角、尺寸标注线、圆、扇形、弓形、弧、圆环、椭圆（`rotationDeg`，0° 即轴对齐）、点名、区域填充（阴影）。正方形是矩形的工具约束（等宽高提交 rectangle），不设类型

**3D：** 单位立方体（体素）、长方体、圆柱、圆锥、球、四棱锥、三棱柱

另：垫图、JSON 说明书 + Prompt、单一编辑器页面、一份说明书一个空间。2D 默认吸附 1、可切 1/2、修饰键关吸附；3D 体素只整数格。打开/保存 JSON；分享链接把说明书压进 URL hash，无账号无云。2D/3D 都是 Y 为高度（不沿用旧工具的 z-up）。许可证 MIT。界面中英 i18n（默认跟浏览器，否则中文），说明书字段永远英文。2D 视口 Konva，3D 视口 Three.js，说明书不绑渲染器。说明书 → 视口单向同步。手势中预览、松手提交一次。视口为命令式投影器（不用 vue-konva 图元树 / TresJS）。第一期几何计算自写纯函数。说明书用 Zod 校验。Pinia 分 document / editor 两块；能用 VueUse 不用手写。分享链接用 lz-string。壳：Vite + Reka UI + Tailwind + vue-i18n。界面中英 i18n，说明书字段永远英文。

## 永远不做

- [ ] 账号
- [ ] 自建云文档 / 云同步

## 第二期

覆盖度依据：2026-09 数学内容审计（`docs/research-math-coverage.md`），缺口按投入产出排序如下。

- [ ] 函数曲线图元：一次/二次/反比例 + 参数滑块（审计缺口 #2）
- [ ] 变换图元：平移/旋转/轴对称/位似（审计缺口 #3）
- [ ] 坐标系/点图元：可创建、可进说明书（审计 #7）
- [ ] 剖切 / 水面（蛋糕、水泥管、浸没）
- [ ] 拆分工具：Clipper2 烘焙式割补，图元 / 线段两种刀具（ADR 0019）
- [ ] 七巧板、钉板、小棒等教具盒
- [ ] 约束几何（平行、垂直自动跟上；审计缺口 #4）
- [ ] 旋转、翻折等时间轴（与变换图元合并考虑，审计缺口 #3）
- [ ] 3D 进阶：棱台/圆台、截面、展开图（审计缺口 #5）
- [ ] 尺规作图步骤分解（审计 #13，依赖步骤/回放概念）
- [ ] 一份说明书里 2D+3D 图元共存
- [ ] 绑定 Geometry Studio 课件字段

已落地移出：~~重叠填充图元~~（ADR 0019，已上线，见 `overlapFill`）；~~度量数值层：角度度数显示、面积/周长标注~~（ADR 0020，已上线，见 `measure`，spec `.scratch/measure-labels/`）。
