# 后期（第一期明确不做）

第一期图元见下节。这里只记已经拍板延后的事，避免范围回潮。

## 第一期图元

**2D：** 线段 / 折线、多边形、圆、扇形、弓形、弧、圆环、椭圆（`rotationDeg`，0° 即轴对齐）、点名、区域填充（阴影）

**3D：** 单位立方体（体素）、长方体、圆柱、圆锥、球、四棱锥、三棱柱

另：垫图、JSON 说明书 + Prompt、单一编辑器页面、一份说明书一个空间。2D 默认吸附 1、可切 1/2、修饰键关吸附；3D 体素只整数格。打开/保存 JSON；分享链接把说明书压进 URL hash，无账号无云。2D/3D 都是 Y 为高度（不沿用旧工具的 z-up）。许可证 MIT。界面中英 i18n（默认跟浏览器，否则中文），说明书字段永远英文。2D 视口 Konva，3D 视口 Three.js，说明书不绑渲染器。说明书 → 视口单向同步。手势中预览、松手提交一次。视口为命令式投影器（不用 vue-konva 图元树 / TresJS）。第一期几何计算自写纯函数。说明书用 Zod 校验。Pinia 分 document / editor 两块；能用 VueUse 不用手写。分享链接用 lz-string。壳：Vite + Reka UI + Tailwind + vue-i18n。界面中英 i18n，说明书字段永远英文。

## 永远不做

- [ ] 账号
- [ ] 自建云文档 / 云同步

## 第二期

- [ ] 剖切 / 水面（蛋糕、水泥管、浸没）
- [ ] Clipper2 多边形布尔（割补、复杂阴影）
- [ ] 七巧板、钉板、小棒等教具盒
- [ ] 约束几何（平行、垂直自动跟上）
- [ ] 旋转、翻折等时间轴
- [ ] 一份说明书里 2D+3D 图元共存
- [ ] 绑定 Geometry Studio 课件字段
